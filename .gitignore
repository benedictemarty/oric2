# Sous-projets ont leur propre git, pas de tracking ici
Phosphoric/
OricOS/

# Mémoire Claude Code (locale par utilisateur)
.claude/

# Caches éditeurs
*.swp
.DS_Store

# Artefacts de build parasites (compilations debug ad-hoc à la racine)
a.out
*.out
*.elf
*.o
